fastapi>=0.115.0
openai>=1.30.0
httpx>=0.27.0
pyyaml>=6.0
pydantic>=2.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
upstash-redis>=1.0.0
pdfminer.six>=20231228
pypdfium2>=4.0.0
Pillow>=10.0.0
google-auth>=2.40.0

# NOTE: markitdown[pdf] は optional (magika/onnxruntime を含み 200MB 超)。
# Vercel Lambda 上限 (245MB) を超えるため本番 requirements からは外し、
# ローカル開発でのみ requirements-dev.txt 経由で導入する。
